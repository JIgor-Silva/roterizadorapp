import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  outerContainer: {
    position: 'relative',
    overflow: 'hidden',
    flex: 1,
  },
  scrollView: {
    height: '100%',
    width: '100%',
  },
});
