const ScrollBar = () => {
  console.warn("O componente ScrollBar não é usado em React Native. Controle a barra de rolagem através das props do ScrollArea (ex: showsVerticalScrollIndicator).");
  return null;
};

export { ScrollBar };
