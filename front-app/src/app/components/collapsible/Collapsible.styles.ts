import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  content: {
    overflow: 'hidden',
  },
  contentInner: {
  },
  measuringContainer: {
    position: 'absolute',
    opacity: 0,
    zIndex: -1,
  },
});
