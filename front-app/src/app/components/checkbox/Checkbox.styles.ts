import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
  },
  containerChecked: {
  },
  containerDisabled: {
  },
  indicator: {
  }
});

export default styles;
