import { StyleSheet } from 'react-native';

const PRIMARY_COLOR = '#007bff'; 
const SECONDARY_COLOR = '#e9ecef'; 
const THUMB_COLOR = '#007bff'; 

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    justifyContent: 'center',
  },
  slider: {
    width: '100%',
    height: 40,
  },
});

export const PRIMARY_COLOR_CONST = PRIMARY_COLOR;
export const SECONDARY_COLOR_CONST = SECONDARY_COLOR;
export const THUMB_COLOR_CONST = THUMB_COLOR;