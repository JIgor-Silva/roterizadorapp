import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  label: {
    fontSize: 14,
    fontWeight: '500',
    lineHeight: 20, 
    color: '#09090b', 
  },
  labelDisabled: {
    opacity: 0.7,
  },
});
